# Runtime dependencies for the Agentic QEM Lab (local-first core, Phases L0–L4).
# Mirrors [project.dependencies] in pyproject.toml. Install dev/cloud extras via:
#   pip install -e ".[dev]"      # tests + lint
#   pip install -e ".[cloud]"    # deferred Phase C5 (AgentCore + alt VLM providers)
amazon-braket-sdk==1.117.3
mitiq==1.0.0
cirq-ionq==1.6.1

numpy>=1.26,<2.1
networkx>=3.0
plotly==6.7.0
kaleido==0.2.1
langchain-core>=1.4
langchain-aws>=1.5
boto3>=1.34
pydantic>=2.6
pyyaml>=6.0
